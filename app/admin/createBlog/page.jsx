import SearchBox from "@/components/AdminComponents/SearchBox";

const page = () => {
    return (
        <div>
            <SearchBox headerText={"Create a Blog"} />
        </div>
    );
};

export default page;
